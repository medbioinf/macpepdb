# macpepdb_web_common

Shared web API request/response types for [`macpepdb`](https://github.com/medbioinf/macpepdb),
used by both its axum-based HTTP backend and its Dioxus frontend so the two stay in sync on the
wire format.
