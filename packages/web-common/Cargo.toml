[package]
name = "macpepdb_web_common"
version = "0.1.0"
edition = "2024"
description = "Shared web API types for macpepdb's backend and frontend"
license = "BSD-3-Clause"
repository = "https://github.com/medbioinf/macpepdb"

[dependencies]
serde = { workspace = true, features = ["derive", "rc"] }

[dev-dependencies]
serde_json = { workspace = true }
